{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-09-29/server.schema.json",
  "name": "io.github.AshMet/vibemap-mcp",
  "description": "Connect AI agents to VibeMap product specs: load personas, features, user stories, acceptance criteria and DB schema as build context; reverse-engineer codebases into specs; sync kanban progress as agents build.",
  "repository": { "url": "https://github.com/AshMet/vibemap-mcp", "source": "github" },
  "version": "2.8.1",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@vibemap.ai/mcp-server",
      "version": "2.8.1",
      "transport": { "type": "stdio" },
      "environmentVariables": [
        { "name": "VIBEMAP_API_KEY", "description": "API key from vibemap.ai -> Account -> MCP (Pro plan)", "isRequired": true, "isSecret": true },
        { "name": "VIBEMAP_BASE_URL", "description": "Defaults to https://vibemap.ai", "isRequired": false }
      ]
    }
  ]
}
