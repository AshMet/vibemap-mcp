node_modules

# Build output is a generated artifact: regenerated by `pnpm build` locally, by
# `prepublishOnly` on npm publish, and by the CI build job. Never commit it —
# a committed stale build once shipped a wrong tool set.
build
